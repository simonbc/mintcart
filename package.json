{
  "name": "nftproducts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npx hardhat compile",
    "dev": "npx hardhat node",
    "migrate:local": "npx hardhat run --network localhost scripts/deploy.js",
    "migrate:rinkeby": "npx hardhat run --network rinkeby scripts/deploy.js",
    "migrate:mumbai": "npx hardhat run --network mumbai scripts/deploy.js",
    "migrate:matic": "npx hardhat run --network matic scripts/deploy.js",
    "test": "npx hardhat test",
    "test:watch": "nodemon -x 'npx hardhat test' -w contracts -w test -e sol"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@openzeppelin/contracts": "^4.5.0",
    "create-next-app": "^12.1.0",
    "dotenv": "^16.0.0",
    "hardhat": "^2.9.3"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.0",
    "@nomiclabs/hardhat-waffle": "^2.0.0",
    "chai": "^4.2.0",
    "ethereum-waffle": "^3.0.0",
    "ethers": "^5.0.0"
  }
}
